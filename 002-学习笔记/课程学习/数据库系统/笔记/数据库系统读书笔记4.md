---
title: 数据库系统读书笔记4
created: 2025-10-11 19:20
tags: [课程, 课程学习, 数据库]
subject: 数据库
modified: 2025-12-28 12:47
---
> 在第一章的时候提到过，[[数据库系统读书笔记1#^1a90c7|数据库管理数据的优势之一是保证了数据的安全性，完整性]]，本章就讨论安全性

## 第四章 数据库安全性

### 4.1 数据库安全性概述

数据库安全性是指保护数据库，以防**数据写漏、篡改、破坏**，这也是衡量数据库系统的一个技术指标。

#### 4.1.1 数据库的不安全因素

对数据库安全性有影响的因素主要有以下几个方面：

1. **非授权用户对数据库的恶意存取和破坏**
2. **数据库中敏感的数据被泄露**
	比如通过SQL注入，使数据库服务器执行未授权的语句
3. **安全环境的脆弱**
	数据库的安全和计算机系统的安全也有关联

#### 4.1.2 安全标准简介

![[安全级别划分.png]]这是美国国防部分指定的标准，考虑方面
	安全策略
	责任
	保证
	文档

![[我国CC安全标准.png]]这是我国采用的CC标准

### 4.2 数据库安全性控制

1. 用户身份识别（身份认证）
2. 访问控制（存取控制）
	1. 定义用户权限
	2. 检查操作权限
	这两点组成了DBMS的安全性系统
	存取控制的常用方法：
		1. 自主访问控制DAC
			通过 SQL 的 GRANT 语句和 REVOKE 语句实现。用户的权限包括数据对象和操作类型
			自主控制访问的授权粒度
				数据对象粒度：数据库、表、属性列、行
				**授权粒度越细，授权子系统就越灵活，能够提供的安全性就越完善**。但另一方面，因数据字典变大变复杂，系统定义与检查权限的开销也会相应地增大
		2. 强制访问控制MAC
			- 定义"向下读"规则：用户可以读取低于或等于自己安全级别的数据
			- 定义"向上写"规则：用户只能写入高于或等于自己安全级别的数据
3. 视图机制
	对用户开放部分数据视图，保证一些和用户能看到的数据和身份对应
4. 数据审计
	这是一种可选的操作，把数据库系统的活动记录到日志，包括服务器事件，系统权限，sql语句
5. 数据加密

```sql
-- GRANT语句的一般格式：
       GRANT <权限>[,<权限>]... 
       [ON <对象类型> <对象名>]
       TO <用户>[,<用户>]...
       [WITH GRANT OPTION];
       
       -- WITH GRANT OPTION子句:
		- 指定：可以再授予
		- 没有指定：不能传播
	  -- 权限可以具体到属性列，比如 UPDATE（SNO）
		  
  -- REVOKE语句的一般格式为：
	  REVOKE <权限>[,<权限>]... 
	  [ON <对象类型> <对象名>]
	  FROM <用户>[,<用户>]...
	  [cascade | restrict];
		-- cascade 级联收回直接或间接从用户处获得的权限
		-- restrict 如果该用户有给别的用户授权，则拒绝收回权限
		-- FROM PUBLIC 回收所有用户
		
-- 创建数据库模式的权限有DBA在创建用户的时候实现
CREATE USER语句格式
              CREATE  USER  <username> 
            ［WITH］［DBA | RESOURCE | CONNECT］
            
-- 反复授权麻烦的时候，可以通过创建角色，指定角色的权限，然后复用角色给用户授权
一、角色的创建
	CREATE  ROLE  <角色名> 

二、给角色授权 
 GRANT  <权限>［，<权限>］… 
 ON <对象类型>对象名  
 TO <角色>［，<角色>］…
 
 三、将一个角色授予其他的角色或用户
	GRANT  <角色1>［，<角色2>］…
	TO  <角色3>［，<用户1>］… 
	［WITH ADMIN OPTION］ 
	
	-- 如果指定了 WITH ADMIN OPTION， 被授予角色的用户可以把角色授予别的用户

四、角色权限的收回 
	REVOKE <权限>［，<权限>］…
	ON <对象类型> <对象名>
	FROM <角色>［，<角色>］…




```

|          |             |               |              |                 |
| -------- | ----------- | ------------- | ------------ | --------------- |
|          |             | 可否执行的操作       |              |                 |
| 拥有的权限    | CREATE USER | CREATE SCHEMA | CREATE TABLE | 登录数据库 执行数据查询和操纵 |
| DBA      | 可以          | 可以            | 可以           | 可以              |
| RESOURCE | 不可以         | 不可以           | 不可以          | 不可以             |
| CONNECT  | 不可以         | 不可以           | 不可以          | 可以，但必须拥有相应权限    |

