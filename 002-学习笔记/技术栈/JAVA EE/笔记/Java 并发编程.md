---
created: 2026-03-22
modified: 2026-03-22
---

# 写在前面

并发编程，一般是指多线程编程，同一段时间内，或者同一时间时刻，运行多个任务。并发编程涉及到对于进程和线程概念的理解，JVM对线程的实现模型，线程的状态，线程如何带调度，多线程什么时候使用，使用的可能会发生的死锁，如何解决死锁问题

# 线程和进程

[[进程和线程#进程和线程的区别是什么|之前的笔记：进程和线程的区别是什么]]

上面的笔记讲解了在操作系统视角下的线程，包括所拥有的资源，存在的形式，通信的方
式。而在JVM中的线程，会稍微有一些区别。

在古早的版本，Java使用的线程，是一种用户级别线程，由用户程序来控制线程执行的生命周期和调用（也就是JVM管理），JDK1.2之后，Java的线程使用的是操作系统提供的内核级别的线程，由OS内核负责管理线程调度。

> 用户级别线程：用户程序管理线程，无法执行异步IO操作，无法使用CPU的多核
> 内核级别线程：操作系统管理线程，可以使用多核，创建和调度的开销比较高，因为涉及到系统调用，CPU切换状态
> 用户级别线程需要和内核级别线程关联映射：
> - 1对1：一个用户线程对应一个内核线程，本质上来说，线程都由OS进行管理
> - 多对1：多个用户线程对应一个内核线程，用户线程完全由用户程序进行管理调度，无法多核执行，并且进程的一个线程阻塞，会导致进程直接被阻塞
> - 多对多：用户进程和OS一起管理线程


# 线程具有的资源

在JVM管理的内存空间中，线程可以共享JVM的堆区和方法区，拥有自己的本地方法栈（Native Method Stack）、虚拟栈、程序计数器

- 虚拟机栈：存放线程调用Java方法的栈帧，包括数据保护和程序流程控制
- 本地方法站：虚拟机使用到的本地方法（Native Method）的栈帧
- PC：保证线程调度之后，能从正确的位置继续执行

- 堆区：JVM中管理的内存空间，存放所有对象
- 方法区：包含类定义信息、字符串常量池、静态变量、JIT编译后的机器代码

# 如何创建线程

- Thread 子类
- Runnable 实现接口
- Callable 实现接口

# 线程生命周期

[[DAY12 多线程编程#**三、线程状态与生命周期** | 线程生命周期]]

Java的生命周期不区分 Running 和 Ready

线程的运行，往往伴随着调度。调度是为了提高资源的利用率和吞吐量，也要求线程的上下文切换。
多线程的运行，也会有导致死锁的情况发生。

死锁产生的必要条件
死锁的预防、避免、检测解决